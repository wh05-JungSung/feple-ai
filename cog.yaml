build:
  gpu: true
  python_version: "3.12"
  system_packages:
    - "ffmpeg"
    - "libsndfile1"
  python_requirements: "requirements.txt"
  run:
    - pip install pyannote.audio==3.1.1 pyannote.core>=5.0.0 pyannote.database>=5.0.0
predict: "predict.py:Predictor"